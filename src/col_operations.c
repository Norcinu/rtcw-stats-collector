#include "col_operations.h"

static struct Record record;
static struct Settings settings;

int intialise_mongo(mongo * conn, const char * username, const char * pw) {
    //record = (Record *)malloc(sizeof(Record));

    return 0;
}
